# 星光 TV 5.7.20

- 版本号：5720；版本名：5.7.20。
- 摸鱼儿配置入口恢复后，优先请求用户保存的原始地址，不再强制替换为已失效的固定镜像。
- 仅针对摸鱼儿相关入口增加备用候选地址；入口返回 HTML 时，提取页面中的配置链接并继续校验。
- 配置响应以实际正文解码结果为准，不再依据扩展名或 MIME 类型拒绝 JSON；继续支持 `.jpg` 路径、Base64、AES 和相对资源地址。
- 所有候选均不可用时，错误信息会列出原始地址及每个候选的 HTTP 状态、HTML、空响应或 JSON 解码失败原因，避免把备用地址误显示为原始链接。

正式产物：`output/XingGuang-5.7.20-arm64-release.apk`。
